---
name: Coding Task
about: Pure implementation work — a feature, refactor, or change with a code deliverable.
title: ""
labels: type:coding-task
---

## Value

<!-- Why this matters — who benefits, and what breaks or stalls without it.
     One or two sentences, no filler.
     Example: "Reviewers currently re-derive test coverage by hand for every
     PR; a coverage delta posted on the PR itself removes that step and
     catches regressions before merge." -->

## Scope

<!-- What's actually being built, and which modules it touches — name real
     files or components, not "the backend." Call out what's explicitly OUT
     of scope if that's likely to be assumed otherwise.
     Example:
     - Add a `coverage-delta` job to `.github/workflows/ci.yml` that runs
       after the existing `test` job and diffs coverage against `main`
     - Post the delta as a PR comment via `scripts/post-coverage-comment.sh`
     - Out of scope: enforcing a minimum coverage threshold — that's a
       follow-up once the numbers are trustworthy -->

## Acceptance Criteria

<!-- Concrete, checkable conditions — not "works well," but "X returns Y
     when Z." Someone other than the author should be able to verify each
     line without guessing.
     Example:
     - [ ] Opening a PR that lowers coverage posts a comment showing the
       before/after percentage and which files lost coverage
     - [ ] A PR that doesn't change coverage posts no comment
     - [ ] The job fails loudly (not silently skipped) if the baseline
       coverage report is missing -->

- [ ]
- [ ]
