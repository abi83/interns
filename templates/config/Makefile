# Entry points the interns coder/reviewer pipeline runs before opening or
# updating a PR, regardless of this repo's language or toolchain. Fill these
# in with the real commands, e.g.:
#
#   test:
#   	npm test
#
#   build:
#   	npm run build
#
# Left empty, both targets are no-ops, and the pipeline treats testing as not
# yet configured for this repo rather than failing every PR on it.

.PHONY: test build

test:

build:
