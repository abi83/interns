[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "interns"
version = "0.1.0"
description = "Core library for the interns system"
requires-python = ">=3.12"
license = "MIT"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/interns"]
